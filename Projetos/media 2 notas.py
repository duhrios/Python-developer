nota_um = float(input())
nota_dois = float(input())
soma = (nota_um + nota_dois) / 2

print ('A primeira nota foi {:.2f} a segunta foi {:.2f} por tanto a sua media é {:.2f}:'.format(nota_um, nota_dois,soma))